use module std::lib;
use module std::lib.io;

let static main(...args) : const *char -> int {
    printfn("Command Line Calculator...");

    e : bool = 0;

    do {
        a, b : real, op : int, ln : *char;

        print("> ");
        ln = readln();

        if !isempty(ln) {
            // todo: end...
        }
    } while (!e);

    return 0;
}